sudo: required
services:
  - docker

before_install:
  - docker build -t mkab/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run mkab/react-text npm test -- --coverage

after_success:
  - docker build -t mkab/multi-client ./client
  - docker build -t mkab/multi-nginx ./nginx
  - docker build -t mkab/multi-server ./server
  - docker build -t mkab/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push mkab/multi-client
  - docker push mkab/multi-nginx
  - docker push mkab/multi-server
  - docker push mkab/multi-worker

